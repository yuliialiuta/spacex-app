import React from "react";
import styled from "styled-components";

const SignInnBtn = styled.button``;

const Buttons: React.FC = () => {
  return (
    <div>
      <SignInnBtn></SignInnBtn>
    </div>
  );
};

export default Buttons;
