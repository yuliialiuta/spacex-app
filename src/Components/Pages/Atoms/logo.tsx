import React from "react";
// import styled from "styled-components";

const Logo: React.FC = () => {
  return (
    <div>
      <img src={"../../pics/logo.svg"} alt="logo" />
    </div>
  );
};

export default Logo;
